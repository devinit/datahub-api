import {IConfig} from 'pg-promise';

// get the connection details from Allan or Donata
export const dw: IConfig = {
    user: 'user',
    database: 'dbname',
    host: 'IP address',
    port: 9999,
    password: 'password'
};
